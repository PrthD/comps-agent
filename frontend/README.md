# Frontend (built in P5)

Vite + React + TypeScript + Tailwind + react-leaflet (BUILD_BRIEF §9). Scaffolded in Phase P5.

**Flow:** input (Upload doc / Paste text / Fill form) → review extracted fields with confidence
chips → confirm → results: conservative defensible value + range + confidence, "valued in X.Xs",
Leaflet map (subject + comp pins, flagged comps marked), sortable comp table, LLM rationale.

Env: `VITE_API_BASE_URL` → the Render backend URL.
