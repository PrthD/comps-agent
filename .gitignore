# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environment / secrets
.env
backend/.env

# Claude Code (keep shared settings.json; ignore local overrides)
.claude/settings.local.json

# Node / frontend
node_modules/
dist/
frontend/dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Data: the bundled comps parquet IS tracked (read-only, shipped in the image).
# Ignore any stray/cache parquet files elsewhere, but keep the bundled one.
*.parquet
!backend/data/kc_sales.parquet
