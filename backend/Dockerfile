# Backend container image — implemented in P6 (deploy). Placeholder during P0 scaffolding.
#
# Plan (BUILD_BRIEF §10): python:3.11-slim base, install uv, `uv sync --no-dev`,
# copy app/ + data/kc_sales.parquet, then run:
#   uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}
